<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="coding_crusade.intellifleetmobile">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_Track"
            android:label="@string/title_activity_activity__track"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:label="Intelli-Fleet Mobile">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity_CProfile"
            android:label="@string/title_activity_activity__cprofile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_PackageStat"
            android:label="@string/title_activity_activity__package_stat"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_PendingDeliveries"
            android:label="@string/title_activity_activity__pending_deliveries"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_UpdateCollection"
            android:label="@string/title_activity_activity__update_collection"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_UpdateDelivery"
            android:label="@string/title_activity_activity__update_delivery"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_booking"
            android:label="@string/title_activity_activity_booking"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_DProfile"
            android:label="@string/title_activity_activity__dprofile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_ManageOrders"
            android:label="@string/title_activity_activity__manage_orders"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_TrackDriver"
            android:label="@string/title_activity_activity__track_driver"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_RemoveDrivers"
            android:label="@string/title_activity_activity__remove_drivers"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_AddDrivers"
            android:label="@string/title_activity_activity__add_drivers"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_MProfile"
            android:label="@string/title_activity_activity__mprofile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_Registration"
            android:label="@string/title_activity_activity__registration"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Activity_Vehicle_CheckOut_CheckIn"
            android:label="@string/title_activity_activity__vehicle__check_out__check_in"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Activity_Routing"
            android:label="@string/title_activity_activity__routing" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Alternative_Routes"
            android:label="@string/title_activity_alternative__routes" />
        <activity
            android:name=".Destination_Route"
            android:label="@string/title_activity_destination__route" />
        <activity
            android:name=".Alternative_Routes2"
            android:label="@string/title_activity_alternative__routes2" />
        <activity
            android:name=".Activity_Back_To_Parking"
            android:label="@string/title_activity__back__to__parking"
            android:theme="@style/AppTheme" />
        <activity android:name=".Activity_Roadside_Assistance" />
        <activity android:name=".Activity_View_Jobs"></activity>
    </application>

</manifest>